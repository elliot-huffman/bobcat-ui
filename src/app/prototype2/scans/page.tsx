'use client';

import type { ReactNode } from 'react';
import { Prototype2Scans } from '../../_components/elements/prototype2/Prototype2Scans';

/** Dedicated route that renders the prototype2 scans screen. */
export default function Prototype2ScansPage(): ReactNode {
    return <Prototype2Scans />;
}
